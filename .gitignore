# Per-user runtime state
nostalgia_launcher_config.json
nostalgia_launcher_hash_cache.json
*.tmp
# Default game folder (created next to the app on first use)
VanillaWoW/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
tests/tmp/*
!tests/tmp/.gitkeep

# PyInstaller build output
build/
dist/
*.AppImage

# Generated macOS icons (packaging/macos/build-icons.sh)
packaging/macos/NostalgiaLauncher.icns
*.iconset/

# Local planning notes (not part of the package)
todo

# Local editor / OS files
.vscode/
.idea/
Thumbs.db
.DS_Store

# Vendored reference source (third-party, not part of the package)
context
