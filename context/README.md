# Context — reference sources

Third-party source trees kept here for reference while working on this
launcher. They are not part of the `nostalgia_launcher` package, not
packaged, and not executed by the app.

## OctoLauncher

A third-party reference launcher (Electron + React + TypeScript) kept here
for reference while working on this project. It is not part of the
`nostalgia_launcher` package, not packaged, and not executed by the app.
See its own `README.md` / `BUILD.md` / `LICENSE` inside `OctoLauncher/`.
